.Rproj.user
.Rhistory
.RData
.Rapp.history
*.xlsx
!input.xlsx

# Reference literature is kept locally, never committed (third-party material).
Literature/

# Office lock/owner files (created while a doc is open in Word/Excel).
~$*

# Release builds (build_release.ps1); published as GitHub Release assets
# instead of being committed.
dist/
